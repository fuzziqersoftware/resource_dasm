# Finder files (macOS)
.DS_Store

# Build products
libresource_file.a
ferazel_render
flashback_decomp
gamma_zee_render
harry_render
hypercard_dasm
infotron_render
lemmings_render
macski_decomp
m68kexec
mshines_render
realmz_dasm
render_bits
render_sprite
resource_dasm

# Files fuzziqersoftware uses during development that wouldn't make sense to run
# anywhere else (and hence are not committed)
release
release.zip
scripts
notes
files

# CMake files
CMakeCache.txt
CMakeFiles
Makefile
CTestTestFile.cmake
Testing
cmake_install.cmake
install_manifest.txt
